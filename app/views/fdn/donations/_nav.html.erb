<ul class="nav nav-underline">
  <li class="nav-item">
    <%= link_to "Contributions", foundation_contributions_path(@foundation), 
        class: "#{request.path == foundation_contributions_path(@foundation)  ? "nav-link active" : "nav-link"}" %>
  </li>
  <li class="nav-item">
    <%= link_to "Commitments", foundation_commitments_path(@foundation), 
        class: "#{request.path == foundation_commitments_path(@foundation)  ? "nav-link active" : "nav-link"}" %>
  </li>
</ul>
