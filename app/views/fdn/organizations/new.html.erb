<%= turbo_frame_tag Organization.new do %>
  <div class="card mb-2">
    <div class="card-header">New Organization</div>
    <div class="card-body pb-2">
      <%= render "form", organization: @organization, form_url: foundation_organizations_path(@foundation) %>
    </div>
  </div>
<% end %>
